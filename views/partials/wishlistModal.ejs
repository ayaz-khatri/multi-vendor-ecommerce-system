<div class="modal fade" id="wishlistModal" tabindex="-1">
    <div class="modal-dialog">
        <form method="POST" action="/wishlist/toggle" class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title">Confirm</h5>
                <button type="button" class="close" data-dismiss="modal">
                    <span>&times;</span>
                </button>
            </div>

            <div class="modal-body" id="wishlistModalText">
                Are you sure?
            </div>

            <!-- Hidden inputs -->
            <input type="hidden" name="productId" id="wishlistProductId">
            <input type="hidden" name="shopId" id="wishlistShopId">
            <input type="hidden" name="vendorId" id="wishlistVendorId">
            <input type="hidden" name="action" id="wishlistAction">

            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-primary">
                    Yes, Continue
                </button>
            </div>

        </form>
    </div>
</div>
